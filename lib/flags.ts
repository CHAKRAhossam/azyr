/* =========================================================================
   DIAGNOSTIC PERFORMANCE
   - SHOW_IMAGES = false  → AUCUNE image n'est chargée (placeholder gris).
                            Sert à vérifier si la lenteur vient des images.
   - SHOW_IMAGES = true   → site normal avec ses images.
   Repasse simplement cette valeur à `true` une fois le test terminé.
   ========================================================================= */
export const SHOW_IMAGES = true;
